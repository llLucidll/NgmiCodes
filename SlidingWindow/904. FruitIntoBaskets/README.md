### Approach


We can only have a maximum of two fruits at a time so we track the current fruit counts in our dictionary and make sure the window is valid.


Time: O(n) we do one pass through the list



Space: O(1) the worst case time complexity is constant


