### Approach


Here we get an added constraint that the two words can possibly be equal, so we need to take care to not return the same index and make changes in the code to account for this



Time: O(N) We do a single pass through the array



Space: O(1)